<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pacmanlike">

    <application
        android:allowBackup="true"
        android:icon="@drawable/basemap"
        android:label="@string/app_name"
        android:roundIcon="@drawable/straight"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pacmanlike">
        <activity android:name=".LevelMaker"
            android:parentActivityName=".SelectionScreen">
        </activity>
        <activity android:name=".GameScreen"
            android:parentActivityName=".SelectionScreen">
        </activity>
        <activity
            android:name=".SelectionScreen"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>